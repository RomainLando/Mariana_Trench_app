import React from 'react'
import { Link } from 'react-router-dom'

export default function UpDownButtons({props}) {
  return (
    <div>
        <button>Up</button>
        <button>Down</button>
    </div>
  )
}
